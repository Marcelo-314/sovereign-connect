# implementation-report.md - MU-017 Canonical Topology SQLite/Flyway Persistence

Package version: v0.2.1
Target: MU-SOV-SC-C-CANONICAL-TOPOLOGY-SQLITE-PERSISTENCE-001
Operational slot: MU-017

---

## 1. Implementation summary

Status:

```text
PASS
```

Branch:

```text
feat/sc-c-mir-017-normalized-topology-sqlite-persistence
```

Commit:

```text
feat(sc-c): add normalized SQLite persistence for canonical topology
```

Short summary:

```text
Implemented normalized SQLite/Flyway topology persistence as the production path for SC-C Base Topology, with dedicated SQLite repositories for structural topology, endpoint health, materialization state and materialization replay.
```

---

## 2. Test evidence

Command:

```text
mvn test
```

Result:

```text
Tests run: 152
Failures: 0
Errors: 0
Skipped: 0
Build: SUCCESS
Finished at: 2026-05-22T16:33:56-03:00
```

Relevant reports:

```text
SQLiteTopologyPersistenceTest: 10 tests, 0 failures
TopologyPersistenceSpringContextTest: 3 tests, 0 failures
Full suite: 152 tests, 0 failures, 0 errors, 0 skipped
```

---

## 3. Files changed

```text
src/main/resources/db/migration/V4__sc_c_normalized_topology_persistence.sql
src/main/java/com/sovereign/connect/adapter/persistence/InMemoryBaseTopologyRepository.java
src/main/java/com/sovereign/connect/adapter/persistence/sqlite/SQLiteBaseTopologyRepository.java
src/main/java/com/sovereign/connect/adapter/persistence/sqlite/SQLiteEndpointHealthRepository.java
src/main/java/com/sovereign/connect/adapter/persistence/sqlite/SQLiteTopologyMaterializationStateRepository.java
src/main/java/com/sovereign/connect/adapter/persistence/sqlite/SQLiteMaterializationDecisionReplayRepository.java
src/main/java/com/sovereign/connect/config/TopologyPersistenceConfiguration.java
src/test/java/com/sovereign/connect/adapter/persistence/sqlite/SQLiteTopologyPersistenceTest.java
src/test/java/com/sovereign/connect/core/topology/TopologyPersistenceSpringContextTest.java
docs/mir/mir-017/implementation-report.md
```

Confirm excluded:

```text
.idea/*: not present in git status
.gitignore unrelated changes: absent
docs/mir/mir-001/*: not present in git status
docs/mir/mir-002/*: not present in git status
#U2014 duplicate artifacts: not present in git status
target/*.sqlite: generated by tests but ignored / not present in git status
```

---

## 4. V4 migration evidence

Migration file:

```text
src/main/resources/db/migration/V4__sc_c_normalized_topology_persistence.sql
```

Tables created:

```text
topology_versions
topology_metadata
rooms
zones
devices
endpoints
capabilities
topology_spatial_relations
topology_snapshots
mutation_records
device_states
endpoint_health
materialization_decision_replay
```

FK / index notes:

```text
rooms/zones/devices/endpoints use FK-safe structural ordering.
endpoint_health and device_states do not FK to endpoints/devices, preserving independent runtime lifecycle.
capabilities use habitat-global capability_id primary key; owner_kind/owner_id are metadata.
relation subject/target indexes are present for CTI-style lookup paths.
```

---

## 5. Normalized persistence evidence

Rows verified for:

```text
rooms: PASS
zones: PASS
devices: PASS
endpoints: PASS
capabilities: PASS
topology_spatial_relations: PASS
```

Authority statement:

```text
Normalized tables are authoritative.
topology_json is compatibility/read model only.
```

Evidence:

```text
SQLiteTopologyPersistenceTest#baseTopologyRowsPersistAndHydrateThroughSQLite
SQLiteTopologyPersistenceTest#topologyJsonIsCompatibilitySnapshotNotAuthority
```

---

## 6. Write pattern evidence

Transaction mechanism used:

```text
TransactionTemplate in SQLiteBaseTopologyRepository.save(...)
```

Delete order implemented:

```text
1. topology_spatial_relations
2. capabilities
3. endpoints
4. devices
5. zones
6. rooms
```

Insert order implemented:

```text
1. topology_versions
2. topology_metadata
3. rooms
4. zones
5. devices
6. endpoints
7. capabilities
8. topology_spatial_relations
9. topology_snapshots
```

State/health preservation:

```text
Structural save does not delete/write device_states: PASS
Structural save does not delete/write endpoint_health: PASS
```

Evidence:

```text
SQLiteTopologyPersistenceTest#structuralSaveDoesNotOverwriteEndpointHealthOrDeviceStateInSQLite
```

---

## 7. Hydration evidence

Full topology hydration source:

```text
Normalized tables, not topology_json.
```

Entity-level query source:

```text
findRoom: normalized rooms + related tables
findZone: normalized zones + related tables
findSpatialRelation: topology_spatial_relations
findSpatialRelationsBySubject: topology_spatial_relations
findLocatedDevices: topology_spatial_relations + devices
findLocatedEndpoints: topology_spatial_relations + endpoints
resolvePrimaryPlacement: topology_spatial_relations
```

Fallbacks:

```text
Endpoint health missing row fallback: EndpointHealth(UNKNOWN, null, "no durable endpoint health row")
```

Evidence:

```text
SQLiteTopologyPersistenceTest#ctiRoomZoneLocatedInQueriesReadFromNormalizedTables
SQLiteTopologyPersistenceTest#coreSnapshotQueryReadsTopologyFromNormalizedSQLite
```

---

## 8. Spring wiring evidence

Beans wired:

```text
BaseTopologyRepository -> SQLiteBaseTopologyRepository
CoreSnapshotReadPort -> SQLiteBaseTopologyRepository
EndpointHealthWritePort -> SQLiteEndpointHealthRepository
TopologyMaterializationStatePort -> SQLiteTopologyMaterializationStateRepository
MaterializationDecisionReplayPort -> SQLiteMaterializationDecisionReplayRepository
BaseTopologyService -> bean
CoreSnapshotQueryService -> bean
TopologyMaterializationService -> bean
```

Production exclusion:

```text
H2BaseTopologyRepository selected in production context: NO
InMemoryBaseTopologyRepository production default: NO
```

Evidence:

```text
TopologyPersistenceSpringContextTest#springContextWiresTopologyPortsToSQLiteRepositories
TopologyPersistenceSpringContextTest#h2BaseTopologyRepositoryIsNotSelectedInProductionContext
TopologyPersistenceSpringContextTest#inMemoryBaseTopologyRepositoryIsNotProductionDefault
```

---

## 9. Outbox/Ledger disposition

Selected option:

```text
Option A - SQLiteScLedgerOutboxRepository production-path coverage added and passed.
```

Evidence:

```text
SQLiteTopologyPersistenceTest#sqliteScLedgerOutboxRepositoryPersistsLedgerAndOutbox
```

`OutboxLedgerStorageSeedTest` remains as legacy H2 seed evidence and also passes.

---

## 10. Jackson shim disposition

Selected option:

```text
Option B - shims retained as bounded technical debt.
```

Rationale:

```text
Existing local classes under src/main/java/com/fasterxml/jackson/annotation remain unchanged. MU-017 did not widen or resolve that dependency-alignment issue.
```

---

## 11. Non-goal confirmation

Confirm not implemented:

```text
SC-C north-facing facade: not implemented
Effective Interaction Boundary: not implemented
View Composer: not implemented
SC-B runtime: not implemented
NATS / JetStream: not implemented
SC-D runtime: not implemented
ActionTemporalPayload: not implemented
command dispatch: not implemented
outbox dispatcher: not implemented
graph database / Kuzu / Neo4j / AGE: not introduced
```

Source scan note:

```text
Matches for ActionTemporalPayload/ActionRequest and JetStream are existing negative assertions in tests, not implementations.
```

---

## 12. Acceptance map result

| AC | Status | Evidence |
|---|---|---|
| AC-017-001 | PASS | V4 migration file exists |
| AC-017-002 | PASS | sqliteNormalizedTopologyFlywayMigrationCreatesRequiredTables |
| AC-017-003 | PASS | baseTopologyRowsPersistAndHydrateThroughSQLite |
| AC-017-004 | PASS | baseTopologyRowsPersistAndHydrateThroughSQLite |
| AC-017-005 | PASS | baseTopologyRowsPersistAndHydrateThroughSQLite |
| AC-017-006 | PASS | baseTopologyRowsPersistAndHydrateThroughSQLite |
| AC-017-007 | PASS | capabilities row assertion + habitat-global PK |
| AC-017-008 | PASS | topology_spatial_relations row assertion |
| AC-017-009 | PASS | topologyJsonIsCompatibilitySnapshotNotAuthority |
| AC-017-010 | PASS | TransactionTemplate source evidence |
| AC-017-011 | PASS | FK-safe delete order source + roundtrip test |
| AC-017-012 | PASS | FK-safe insert order source + roundtrip test |
| AC-017-013 | PASS | structuralSaveDoesNotOverwriteEndpointHealthOrDeviceStateInSQLite |
| AC-017-014 | PASS | structuralSaveDoesNotOverwriteEndpointHealthOrDeviceStateInSQLite |
| AC-017-015 | PASS | ctiRoomZoneLocatedInQueriesReadFromNormalizedTables |
| AC-017-016 | PASS | baseTopologyRowsPersistAndHydrateThroughSQLite |
| AC-017-017 | PASS | coreSnapshotQueryReadsTopologyFromNormalizedSQLite |
| AC-017-018 | PASS | structuralSaveDoesNotOverwriteEndpointHealthOrDeviceStateInSQLite |
| AC-017-019 | PASS | materializationDecisionReplayAndDuplicateFactUseSQLite |
| AC-017-020 | PASS | materializationDecisionReplayAndDuplicateFactUseSQLite |
| AC-017-021 | PASS | ctiRoomZoneLocatedInQueriesReadFromNormalizedTables; mutationRecordsPersistAndRecoverThroughSQLite |
| AC-017-022 | PASS | domain services unchanged; SQLite code stays in adapter/config |
| AC-017-023 | PASS | full mvn test |
| AC-017-024 | PASS | noTopologyRepositoryImplementsLedgerOrOutboxPorts |
| AC-017-025 | PASS | sqliteScLedgerOutboxRepositoryPersistsLedgerAndOutbox |
| AC-017-026 | PASS | Jackson shim disposition recorded |
| AC-017-027 | PASS | git status scoped to MIR-017 changes; ignored target sqlite not included |
| AC-017-028 | PASS | no graph DB dependency or implementation introduced |
| AC-017-029 | PASS | non-goal source scan |
| AC-017-030 | PASS | changed-file list above |

---

## 13. Final disposition

```text
MU-017 implementation disposition:
  Validated L4 candidate
```
