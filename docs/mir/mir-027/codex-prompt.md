# codex-prompt — MIR-SOV-SC-B-DISPATCH-OBSERVATION-PERSISTENCE-001

```text
Document ID:  codex-prompt-MIR-SOV-SC-B-DISPATCH-OBSERVATION-PERSISTENCE-001
Version:      v0.1.0
Status:       Execution Prompt
Date:         2026-05-30
Corpus:       Sovereign Connect
Plane:        SC-B
MU Slot:      MU-027
Scope:        Dispatch Observation Persistence only
```

You are implementing `MIR-SOV-SC-B-DISPATCH-OBSERVATION-PERSISTENCE-001`.

Use the files in `docs/mir/mir-027/` as normative input:

```text
MIR-SOV-SC-B-DISPATCH-OBSERVATION-PERSISTENCE-001.md
context.md
code-surface-audit.md
acceptance-map.md
```

## Goal

Implement H3:

```text
Persist SC-B DispatchObservationRecord through JDBC/Flyway-compatible storage.
```

Do not broaden scope.

---

## Required production changes

Create:

```text
src/main/java/com/sovereign/connect/bus/runtime/persistence/JdbcDispatchObservationRepository.java
src/main/resources/db/migration/V101__sc_b_dispatch_observation_persistence.sql
```

`JdbcDispatchObservationRepository` must implement the existing `DispatchObservationPort` without changing that port.

DDL must create `sc_b_dispatch_observations` with FK to `sc_b_dispatch_records(dispatch_record_id)`.

Use exactly the DDL from `context.md §4` unless a compile/runtime issue forces a deviation. Record any deviation in `implementation-report.md`.

---

## Required test changes

Create:

```text
src/test/java/com/sovereign/connect/bus/runtime/persistence/JdbcDispatchObservationRepositoryTest.java
src/test/java/com/sovereign/connect/bus/runtime/dispatch/RuntimeDispatchObservationPersistenceTest.java
src/test/java/com/sovereign/connect/bus/ScBusObservationPersistenceArchitectureTest.java
```

Update:

```text
src/test/java/com/sovereign/connect/bus/ScBusOutboxBridgeArchitectureTest.java
```

Mandatory update:

```text
ScBusOutboxBridgeArchitectureTest.h2DoesNotAddScBMigrations()
```

must allow both:

```text
V100__sc_b_dispatch_state_persistence.sql
V101__sc_b_dispatch_observation_persistence.sql
```

This is `ACTION-H3-001` and must be in the implementation commit.

---

## Exact test list

`JdbcDispatchObservationRepositoryTest` must contain these 11 tests:

```text
recordPersistsObservationWithAllMandatoryFields
recordRejectsNullObservation
recordRejectsNullObservationId
recordRejectsNullDispatchRecordId
recordRejectsNullState
recordRejectsNullObservedAt
observationsForUnknownDispatchRecordIdReturnsEmptyList
observationSurvivesRepositoryRecreation
multipleObservationsReturnedInObservedAtOrder
noHandlerObservationPersistsCodeAndSanitizedReason
deliveryFailedObservationPersistsCodeAndSanitizedReason
```

`RuntimeDispatchObservationPersistenceTest` must contain these 8 tests:

```text
claimEmitsPersistentClaimedObservation
dispatchEmitsPersistentDispatchingAndDispatchedObservations
noHandlerEmitsPersistentDeliveryFailedObservation
retryEmitsPersistentRetryScheduledObservation
exhaustionEmitsPersistentExhaustedObservation
supersedeCancellationEmitsPersistentCancelledObservation
dispatchedObservationIsTechnicalNotSemantic
exhaustedObservationIsTechnicalNotSemantic
```

`ScBusObservationPersistenceArchitectureTest` must contain these 5 tests:

```text
busRuntimePersistenceDoesNotImportCore
coreDoesNotImportBusRuntime
noBrokerDependencyIntroduced
scBObservationMigrationUsesV101
scBObservationMigrationCreatesOnlyScBTables
```

---

## Implementation rules

1. Preserve existing `DispatchObservationRecord` shape.
2. Preserve existing `DispatchObservationPort` shape.
3. Preserve `InMemoryDispatchObservationRepository`.
4. Do not add `CANDIDATE_SELECTED`; there is no runtime hook for it.
5. Preserve `observationId` generated by `RuntimeDispatchService`; do not replace it inside JDBC repository.
6. Persist `observedAt` as epoch milliseconds.
7. Return observations ordered by `observed_at_ms ASC, observation_id ASC`.
8. Use raw `SQLiteDataSource + Flyway.configure(...).locations("classpath:db/migration")` in tests.
9. Do not use `PerConnectionPragmaDataSource` from adapter test support.
10. Do not add JPA.
11. Do not add broker dependencies.
12. Do not import `core.*` from `bus.*` production code.
13. Do not import `bus.runtime.*` from `core.*` production code.
14. Do not mutate `OutboxEntryStatus` or SC-C ledger records.

---

## Stop conditions

Run in order:

```bash
mvn -q compile
```

Expected: build success.

```bash
mvn -q test -Dtest="JdbcDispatchObservationRepositoryTest"
```

Expected: 11 tests, 0 failures, 0 errors.

```bash
mvn -q test -Dtest="RuntimeDispatchObservationPersistenceTest,ScBusObservationPersistenceArchitectureTest,ScBusHardeningArchitectureTest,ScBusOutboxBridgeArchitectureTest"
```

Expected: all pass.

```bash
mvn -q test
```

Expected:

```text
>= 378 tests, 0 failures, 0 errors, 0 skipped
```

If a stop fails, fix within H3 scope. Do not implement lifecycle, NATS, ScDeliveryError event emission, SC-D adapter execution, or semantic terminal-state behavior.

---

## Implementation report

Update:

```text
docs/mir/mir-027/implementation-report.md
```

Record:

```text
branch
commit hash(es)
changed files
test commands
test results
AC mapping summary
whether V101 has FK to sc_b_dispatch_records
confirmation that CANDIDATE_SELECTED remains reserved/not emitted
retained debts
```

Suggested branch:

```text
feat/sc-b-mir-027-dispatch-observation-persistence
```

Suggested implementation commit:

```text
feat(sc-b): persist dispatch observations
```
